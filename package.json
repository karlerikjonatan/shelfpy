{
  "name": "shelfpy",
  "version": "1.0.0",
  "description": "Generates a CSV of book listings from Sellpy matching a curated list of authors.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "start:env": "node --env-file=.env src/index.js",
    "creds": "node -e \"import('./src/sellpy.js').then(m=>m.resolveCredentials().then(console.log))\""
  }
}
